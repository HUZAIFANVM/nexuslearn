# Frontend env — Vite bakes these into the bundle at BUILD time, not runtime.
# In production: set these as BUILD args on your hosting platform.

VITE_API_URL=http://localhost:8000        # In prod: https://api.your-domain.com
VITE_GOOGLE_CLIENT_ID=                    # Same Client ID as backend's GOOGLE_CLIENT_ID
